# ============================================================
# RoadScan-MA — requirements.txt FINAL (unifié)
# Fusion : dépendances entraînement + dashboard Streamlit
# Python 3.10+ | CPU suffisant (pas de GPU requis en inférence)
# ============================================================

# ── YOLO & Computer Vision ──────────────────────────────────
ultralytics>=8.0.0              # YOLOv8 — entraînement + inférence
opencv-python-headless>=4.8.0  # OpenCV sans GUI (headless Streamlit)
Pillow>=10.0.0                  # Manipulation images

# ── Dashboard ───────────────────────────────────────────────
streamlit>=1.28.0
streamlit-folium>=0.15.0        # Intégration carte Folium dans Streamlit

# ── Cartographie ────────────────────────────────────────────
folium>=0.14.0                  # Cartes interactives

# ── Data & Calcul ───────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0               # Graphiques (utilisé aussi en entraînement)
seaborn>=0.12.0                 # Graphiques avancés

# ── GPS / Metadata ──────────────────────────────────────────
exifread>=3.0.0                 # Lecture EXIF/GPS depuis images smartphone
gpxpy>=1.5.0                    # Lecture fichiers GPX (trajets iPhone)

# ── PDF & Rapport ───────────────────────────────────────────
reportlab>=4.0.0                # Génération rapports PDF municipalité

# ── Utilitaires ─────────────────────────────────────────────
python-dotenv>=1.0.0            # Variables d'environnement
tqdm>=4.65.0                    # Barres de progression
requests>=2.31.0                # Requêtes HTTP

# ── Notes CPU ───────────────────────────────────────────────
# PyTorch CPU-only installé automatiquement par ultralytics
# Pour forcer CPU-only explicitement, décommenter :
# --index-url https://download.pytorch.org/whl/cpu
# torch>=2.0.0
# torchvision>=0.15.0
